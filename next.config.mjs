/** @type {import('next').NextConfig} */
const nextConfig = {
    eslint: {
      // Warning: This allows you to continue despite errors
      ignoreDuringBuilds: true,
    },
  };
  
  module.exports = nextConfig;